#ifndef GLOBAL_H
#define GLOBAL_H

const extern int windowWidth;
const extern int windowHeight;

const extern int gameWidth;
const extern int sidebarWidth;

const extern int paddleWidth;
const extern int paddleHeight;
const extern int paddleSpeed;

const extern int initialRows;
const extern int initialCols;

int extern FPS;
int extern speed;
int extern bufferSize;

bool extern splashScreen;
bool extern chooseType;
bool extern basic;
bool extern gameOver;
int extern score;
int extern level;

#endif